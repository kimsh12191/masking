"""학습 데이터 구축.

이 패키지는 **추론에 관여하지 않는다.** VLM 의 좌표 능력을 끌어올리기 위한
학습 데이터를 만드는 도구만 들어 있다.

무엇을 가르치려는 것인가
------------------------

VLM 은 "여기에 이런 개인정보가 있다" 는 판단은 잘 하는데, 그 위치를 숫자로
정확히 찍지 못한다. 비전 토큰 1개가 32×32px 을 담당하므로 사전학습된 상태의
분해능이 300dpi 한글 한 줄(약 35px)과 비슷하고, 그래서 박스가 한 줄쯤 밀린다.
프롬프트로는 못 고친다 — 패치 크기는 아키텍처다.

고칠 방법은 **그 축만 학습**하는 것이다. 그리고 라벨은 사람이 만들 필요가 없다:

    문서 이미지  →  OCR  →  (텍스트, 정확한 박스)

OCR 은 좌표를 정확히 준다. 그걸 그대로 정답으로 쓴다.

왜 추론에서 못 쓰던 OCR 을 학습에는 쓰는가
------------------------------------------

추론 시점에 OCR 박스를 쓰려면 **VLM 이 읽은 값과 OCR 박스를 이어야** 하는데,
그 열쇠가 텍스트라서 한쪽이 한 글자만 틀려도 통째로 끊긴다. 매번 성공해야 하는
일이라 실패가 곧 그 항목의 실패다.

학습에서는 그 이음이 필요 없다. **OCR 결과 자체가 정답**이고, 미심쩍은 건
버리면 된다. 같은 도구를 오류가 치명적인 자리에서 오류가 싼 자리로 옮기는 것이
이 패키지의 요점이다.

가장 조심할 것
--------------

**좌표 변환 경로가 추론과 1픽셀이라도 다르면 안 된다.** 틀린 좌표를 학습시키는
것은 지금보다 나쁘고, 눈으로는 보이지 않는다. 그래서 ``dataset`` 은 좌표를
직접 계산하지 않고 ``detect`` 의 디코딩 함수를 **역으로** 쓰며, 만든 라벨을
다시 추론 경로로 통과시켜 원래 박스로 돌아오는지 검산한다.
"""

from .dataset import (
    GroundingConfig,
    TileSample,
    build_tile_sample,
    roundtrip,
    scale_regions,
    to_permille,
)

__all__ = [
    "GroundingConfig",
    "TileSample",
    "build_tile_sample",
    "roundtrip",
    "scale_regions",
    "to_permille",
]
