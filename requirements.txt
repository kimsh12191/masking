# ---------------------------------------------------------------------------
# OCR
# ---------------------------------------------------------------------------
# 버전을 반드시 핀 고정할 것. PaddleOCR 2.x 와 3.x 는 API 가 크게 다르다.
# 이 코드는 2.x 계열 인터페이스(det_db_thresh, show_log, PaddleOCR.ocr(cls=))
# 를 기준으로 작성되었다.
paddleocr==2.9.1

# paddlepaddle 은 CUDA 버전에 맞는 휠을 별도 설치해야 한다.
#   CUDA 11.8:
#     pip install paddlepaddle-gpu==2.6.2.post118 \
#       -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
#   CUDA 12.x:
#     pip install paddlepaddle-gpu==2.6.2 \
#       -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
#   CPU 전용:
#     pip install paddlepaddle==2.6.2
# 폐쇄망에서는 위 휠을 미리 내려 반입할 것.

# ---------------------------------------------------------------------------
# 이미지 처리
# ---------------------------------------------------------------------------
opencv-python==4.10.0.84
Pillow==10.4.0
numpy>=1.24,<2.0          # paddlepaddle 2.6.x 는 numpy 2.x 와 호환되지 않는다

# ---------------------------------------------------------------------------
# LLM (vLLM OpenAI 호환 API 호출용 클라이언트)
# ---------------------------------------------------------------------------
openai>=1.40,<2.0

# vLLM 은 추론 서버 쪽에 설치한다 (이 패키지의 런타임 의존성이 아니다):
#   pip install vllm>=0.8.0
#   pip install xgrammar          # guided decoding 백엔드

# ---------------------------------------------------------------------------
# 개발/테스트
# ---------------------------------------------------------------------------
pytest>=8.0
ruff>=0.6
